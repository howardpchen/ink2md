# Environment variables consumed by the ink2md systemd service.
# Copy this file to /etc/ink2md/env and update the values before enabling
# the unit.

# Gemini API key for the LLM integration. Comment out if using the simple provider.
GEMINI_API_KEY=""

# Absolute paths to Google Drive OAuth credentials and token cache. The service
# user must have read access to the secrets file and read/write access to the
# token file.
GOOGLE_DRIVE_CLIENT_SECRETS="/etc/ink2md/credentials/client_secrets.json"
GOOGLE_DRIVE_TOKEN_PATH="/var/lib/ink2md/google_drive_token.json"

# Optional override for the temporary directory used when uploading PDFs to the
# LLM provider. Point this at a partition with sufficient free space.
# TMPDIR="/var/tmp/ink2md"
